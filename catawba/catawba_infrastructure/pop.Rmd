---
title: "population"
output: html_document
date: "`r Sys.Date()`"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r packages}

library(tidyverse)
library(tidycensus)
library(dots)

census_api_key("e13d5be5cb48d927009e0dca0af99d21918d514f", install = TRUE)


```

```{r data}

dat <- data("mecklenburg")

munis_watershed <- rbind(
  county_subdivisions(state="North Carolina"), 
  county_subdivisions(state="South Carolina")
  ) %>%
  st_transform(crs="EPSG:4326")

NC <- get_estimates(geography= "county", product = "characteristics", variables = "all", breakdown = "SEX", breakdown_lables = TRUE, state = "North Carolina", output = "wide", geometry = TRUE, keep_geo_vars = TRUE)

SC <- get_estimates(geography= "county", product = "characteristics", variables = "all", breakdown = "SEX", breakdown_lables = TRUE, state = "South Carolina", output = "wide", geometry = TRUE, keep_geo_vars = TRUE)

dots::dots_points(shp = NC, cols = value, divisor = 1250) 

```